## Local environment for the Rules add-on

# Port to bind locally
ADDON_PORT=8080

# Base URL the SDK uses to build the runtime manifest (change to your ngrok https URL when exposing)
ADDON_BASE_URL=http://localhost:8080/rules

# Apply actions (idempotent) on matching webhooks. Omit/false to log only.
RULES_APPLY_CHANGES=false

# Optional: choose a persistent rules store (JDBC). If absent, in-memory store is used.
# RULES_DB_URL=jdbc:postgresql://localhost/rules
# RULES_DB_USERNAME=rules
# RULES_DB_PASSWORD=secret

# Optional: middleware
# ADDON_FRAME_ANCESTORS='self' https://*.clockify.me
# ADDON_RATE_LIMIT=10
# ADDON_LIMIT_BY=workspace
# ADDON_CORS_ORIGINS=https://app.clockify.me
# ADDON_CORS_ALLOW_CREDENTIALS=true

# Local preload for demo (helps tests and webhook simulation)
# CLOCKIFY_WORKSPACE_ID=ws-local
# CLOCKIFY_INSTALLATION_TOKEN=raw-installation-jwt


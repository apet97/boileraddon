# Example environment for local development
# Copy to .env and fill in values.
ADDON_PORT=8080
ADDON_BASE_URL=http://localhost:8080/auto-tag-assistant
# Webhook/auth settings
# Canonical signature header is Clockify-Signature (alternates are temporarily accepted, logged, and counted)
# JWT issuer configuration (prefer EXPECTED_ISS; legacy EXPECT_ISS is deprecated)
CLOCKIFY_JWT_EXPECTED_ISS=clockify
# Optional: max clock skew seconds for JWT exp checks (default 30)
JWT_MAX_CLOCK_SKEW_SECONDS=30
# HMAC compatibility is OFF by default; enable only for dev if needed
# ADDON_AUTH_COMPAT=HMAC
# Database DSN if using a persistent token store
DB_URL=jdbc:postgresql://localhost:5432/clockify_addon
DB_USERNAME=replace_me
DB_PASSWORD=replace_me_securely
ENV=dev
# ADDON_SKIP_SIGNATURE_VERIFY is only honored in dev environments
ADDON_SKIP_SIGNATURE_VERIFY=true
